# 2026-08-24 工作日志

## 今日完成

- **实现 `get_academic_calendar` 工具**（新文件 + 注册 + 部署）
  - 新建 `backend/lambdas/ai-agent/tools/campus/getAcademicCalendar.ts`
  - 探测两个 UChicago 日历源：`registrar.uchicago.edu/calendars/` 无 .ics；`collegecatalog.uchicago.edu` 本地网络不可达但 Lambda 环境可访问
  - 数据源：优先 `collegecatalog.uchicago.edu/thecollege/academiccalendar/` HTML 表格，fallback `registrar.uchicago.edu/calendars/`
  - HTML 解析：按 `<h2>–<h5>` 切割 quarter section，regex 解析 `<tr><td>` 行，支持单日期、跨月区间、同月区间三种日期格式
  - 时区安全：全部手动构造 `YYYY-MM-DD`，不经 `new Date(str)` UTC 转换（避免 off-by-one）
  - `kind` 分类：按 label 关键词映射 instruction/exams/breaks/deadlines/other
  - S3 缓存 7 天，key `cache/academic-calendar/{academicYear}.json`
  - 解析失败返回 `found: false + sourceUrl`，不提供静态兜底
  - `agent.ts`：新增 import、CAMPUS_TOOLS 工具 spec、dispatch case
  - `handler.ts`：`PASSTHROUGH_DATA_TOOLS` 加入 `'get_academic_calendar'`
  - 部署：`pnpm tsc --noEmit` 通过，esbuild bundle 2.2MB，Lambda 已更新（us-east-1）

## 关键决定

- 不创建 shared iCal 模块：探测结果显示无 .ics 源，HTML 路径不需要 VEVENT 解析器，与 getCampusEvents 保持工具自包含原则
- 最小结果阈值设为 4 条（`entries.length >= 4`）：避免把解析失败但返回空表的 HTML 当作成功结果写入缓存
- collegecatalog 是首选源（HTML 表格结构更稳定），registrar 作为 fallback

- **修复 CHRS 字段缺失 bug**（agent 错误回退至全市 ICL 图层）
  - `buildingFields.ts`：`BUILDING_FIELD_ALLOWLIST` 加入 `'CHRS'`
  - `queryBuildingAttributes.ts`：`FIELD_ALIASES` 加入 `chrs`、`historic`、`rating` 三条映射
  - `agent.ts`：工具 spec 描述加入 CHRS 字段说明（categorical string orange/red/yellow/green）；系统提示加两条约束：禁止对校园历史建筑问题回退到 ICL 图层、全市图层校园查询上限 ~50 要素
  - 部署：tsc 通过，esbuild 2.2MB，Lambda us-east-1 已更新

## 遗留 / 下一步

- [ ] 验证 CHRS 三问（prod）：
  1. `where are the CHRS orange buildings on campus?` → 校园内 orange 建筑（十几个量级，非 291）
  2. `which campus buildings are Chicago Landmarks?` → CHRS=red，非 ICL 图层
  3. `show me landmarks near campus` → 此时才允许 ICL，但仍需距离裁剪
- [ ] 前端建筑弹窗加 Historic rating 行（列名已确认为 `CHRS`，按 orange/red/yellow 配色）
- [ ] 验证 get_academic_calendar 四问（prod）：
  1. `when does the fall semester begin?` → 具体日期 + source
  2. `when is winter break?` → 区间日期
  3. `when are final exams this quarter?` → exam week
  4. `when is the last day to drop a class?` → deadline（若源里没有则明确说没有）
- [ ] 若 collegecatalog HTML 结构与预期不符，根据实际抓取结果调整解析器
- [ ] 前端日历卡（学期时间轴）— 等后端验证 entries 形状后做
- [ ] CloudFront `E3J65QFHW23IJZ` 待 IT 确认改造方案后恢复
- [ ] top-N bug 前端三验（prod）：
  - "show me top 3 buildings with the highest FCI value" → 3 FEATURES
  - "what are the 5 tallest buildings on campus" → 5 FEATURES
  - "which buildings were completed before 1930" → 数量与文字一致
