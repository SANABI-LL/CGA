# 2026-08-10 工作日志

## 今日完成

### A2：Digest Cron 健康确认
- 读取 `s3://campusgeo-geodata-491117467175/digest/history.json` 确认：自 2026-07-16 起每天 07:00 UTC 均有执行记录，今日(08-10)也正常触发
- 无需修改代码，EventBridge + Lambda 运行正常

### A3：去 ESRI 化 — bike_racks / parking 上传
- 从 ArcGIS FeatureServer 直接导出 GeoJSON：
  - `bike_racks`：layer 395，164 个 Point feature，上传到 `s3://.../layers/bike_racks.geojson`
  - `parking`：layer 259（原文档错误记录为 10），18 个 feature，上传到 `s3://.../layers/parking.geojson`
- 修正 `backend/lambdas/arcgis-proxy/handler.ts`：`parking` endpoint FeatureServer/10 → /259
- 代码侧早已迁移到 S3（`queryS3Layer.ts`），此次仅补充两个缺失的数据文件

### Bike Rack 地图渲染修复
- **根本原因**：`inject-backend.js` 对所有非树木查询均设 `sc.kind = 'buildings'`，触发 `bldg-hit-fill`（fill layer），该 layer 只渲染 Polygon，Point feature 被静默忽略
- **修复内容**（`inject-backend.js`）：
  1. `isPointLayer` 检测：读取第一个 feature 的 `geometry.type`（用 `features` 变量，避开 `coloredFeatures` 的 TDZ）
  2. `sc.kind = isTree ? 'trees' : isPointLayer ? 'nearby' : 'buildings'`
  3. `addPointLayer()` 函数：在 `__cgShowMapBtn` 回调里动态添加 MapLibre `circle` layer（`nearby-circle`/`nearby-src`），maroon 圆点，retry 机制等待 style 加载
- **缓存问题**：`CampusGeo-with-Backend.html` 的 `<script src="inject-backend.js">` 改为 `inject-backend.js?v=1786373483`
- **验证**：通过 monkey-patch `__cgBackend` 用真实 React setters 发起查询，确认 5 个 bike rack 以 maroon 圆点正确显示在 Regenstein Library 附近地图上

## 关键决定

- `isPointLayer` 检测放在 `features` 定义后、`coloredFeatures` 声明前，避免 let TDZ ReferenceError（首次放在 `coloredFeatures` 后导致了 `Cannot access 'coloredFeatures' before initialization`）
- `nearby-circle` layer 用 retry 轮询（最多 30 次，200ms 间隔）等待 `map.isStyleLoaded()`，因为 MapLibre 的 `composing` 阶段 style 未必已完成加载
- parking layer ID 错误（10 vs 259）靠直接查询 FeatureServer layer list 发现，非猜测

## 遗留 / 下一步

- [ ] `preview_fill` 工具无法触发 React controlled input 的 `onChange`，每次验证需要用 monkey-patch `__cgBackend`。考虑是否在 inject-backend.js 里暴露 `window.__cgSubmit(query)` 辅助函数，方便测试
- [ ] `build-with-backend.mjs` 每次修改 `inject-backend.js` 后需重新运行（patch 数量已达 24 个），现在还不需要重跑（inject-backend.js 是外部脚本，不进 bundle）
- [ ] 6 条 `tools.functional.test.ts` 失败（pre-existing，`gis_output/geojson/` 目录缺失），不影响当前功能，后续补充 fixture
- [ ] Phase 2：BasicAuth on CloudFront + `CampusGeo-with-Backend.html` 上线
