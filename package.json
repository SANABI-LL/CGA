{
  "name": "campusgeo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm --filter @campusgeo/web dev",
    "dev:server": "pnpm --filter @campusgeo/dev-server dev",
    "build": "pnpm --filter @campusgeo/web build",
    "build:all": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
